{
    "name": "crm",
    "version": "0.0.1",
    "type": "module",
    "scripts": {
        "dev": "bun run src/server.ts",
        "test": "bun test",
        "bench": "bun run bench/runner.ts",
        "typecheck": "tsc --noEmit",
        "bench:save": "bun run bench/runner.ts --save",
        "client": "bun run --cwd client dev"
    },
    "dependencies": {
        "ajv": "^8.20.0"
    },
    "devDependencies": {
        "@types/bun": "^1.3.14",
        "@types/node": "^25.9.1",
        "typescript": "^6.0.3"
    }
}
